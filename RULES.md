# MAAGAR RULES — SOURCE OF TRUTH

Repository: `yanivmizrachiy/maagar`

מסמך זה הוא מקור האמת המחייב של הריפו.
כל GPT, Codex, סוכן AI, סקריפט, פעולה ידנית או אתר עתידי חייבים להתחיל מקריאת הקובץ הזה.

יניב הוא הסמכות היחידה לשינוי מטרות, מבנה, סיווגים, עיצוב ותוכן.

---

## 1. מטרת הריפו

הריפו הוא מאגר אמיתי, מסודר וגדול של קבצי מתמטיקה, והוא גם צריך להיות מוכן להפוך לאתר אינטרנט גדול להצגת הקבצים.

הריפו מיועד ל:
- שמירת קבצים אמיתיים בלבד.
- ארגון קבצים ומשימות לפי שכבה, תחום, נושא, סוג, מחבר, שנה, רמה וסיווגים נוספים.
- שליפה חכמה לפי מטא־דאטה.
- מניעת כפילויות לפי תוכן.
- הכנה לאתר עברי RTL גדול ונוח.
- הצגת קבצים באתר באמצעות הטמעה כאשר אפשר, או באמצעות קישור כאשר הטמעה אינה אפשרית.

אסור להשתמש בדמו, תוכן מומצא, קבצים פיקטיביים או מטא־דאטה מנוחש.

---

## 2. אמת בלבד

- כל קובץ באינדקס חייב להיות קובץ אמיתי או קישור אמיתי.
- אין ליצור רשומה למשימה שלא קיימת.
- אין להמציא מחבר, שנה, רמה, כיתה, נושא או סוג.
- אם פרט לא ידוע — יש לכתוב `unknown`.
- אם הסיווג לא ברור — יש להעביר ל־`review/pending_clarification/` או לסמן `unknown`.
- קטגוריות ריקות מותר להציג באתר העתידי, אך יש להציג בהן הודעה אמיתית שאין עדיין קבצים משויכים.

---

## 3. מבנה עמוד ראשי באתר העתידי

העמוד הראשי חייב לכלול ארבעה כפתורים ראשיים:

1. שכבת ז׳
2. שכבת ח׳
3. שכבת ט׳
4. חטיבה עליונה

כל הכפתורים חייבים להיות:
- גדולים וברורים.
- בעברית RTL.
- מותאמים למחשב ולנייד.
- בעיצוב פרימיום.
- מובחנים מאוד זה מזה מבחינה ויזואלית.

---

## 4. מבנה שכבות ז׳, ח׳, ט׳

בלחיצה על שכבת ז׳, ח׳ או ט׳, האתר יציג ארבעה אזורים קבועים:

1. אלגברה
2. גיאומטריה
3. משימות מסכמות
4. מבחנים

קובץ יכול להיות מתאים ליותר מכיתה אחת וליותר מנושא אחד, אבל נשמר פיזית פעם אחת בלבד.
השיוך הכפול נעשה דרך `metadata/index.json`, לא דרך שכפול קבצים.

---

## 5. מבנה חטיבה עליונה

בעמוד הראשי יש כפתור `חטיבה עליונה`.
בלחיצה עליו נפתח מסך חדש עם שלושה כפתורים:

1. בגרות במתמטיקה ברמת שלוש יחידות
2. בגרות במתמטיקה ברמת ארבע יחידות
3. בגרות במתמטיקה ברמת חמש יחידות

כל רמה חייבת לקבל צבעים ורקע שונים לגמרי:

- שלוש יחידות: טורקיז / אמרלד / ים — `#0F766E`, `#14B8A6`, `#ECFDF5`.
- ארבע יחידות: כחול רויאל / אינדיגו — `#1D4ED8`, `#4F46E5`, `#EEF2FF`.
- חמש יחידות: ארגמן / זהב / כתום עמוק — `#9F1239`, `#F97316`, `#FFF7ED`.

הצבעים הם כלל עיצוב לאתר העתידי, לא נתון מתמטי ולא מטא־דאטה של קובץ.

---

## 6. סיווג חובה לכל קובץ, קישור או משימה

כל פריט במאגר צריך לקבל כמה שיותר שדות אמיתיים מתוך הרשימה הבאה:

- `id` — מזהה יציב וייחודי.
- `title` — שם המשימה/הקובץ לתצוגה.
- `path` — מיקום פיזי בריפו, אם זה קובץ.
- `file_name` — שם הקובץ.
- `extension` — סיומת.
- `content_hash` — hash תוכן למניעת כפילויות.
- `school_stage` — `middle-school`, `high-school`, `unknown`.
- `grade` — כיתה עיקרית.
- `grades` — רשימת כיתות אם מתאים ליותר מכיתה אחת.
- `unit_level` — `3-unit`, `4-unit`, `5-unit`, `unknown`.
- `track` — `regular`, `bagrut`, `unknown`.
- `primary_category` — `algebra`, `geometry`, `summaries`, `exams`, `uncategorized`, `unknown`.
- `topics` — רשימת נושאים.
- `document_type` — עבודה, מבחן, קישור, משימה מתוקשבת, משימה להדפסה, עבודת סיכום ועוד.
- `exam_kind` — סוג מבחן אם ידוע.
- `bagrut_questionnaire` — שאלון בגרות אם ידוע.
- `year` — שנה אם ידועה.
- `author` — מחבר אם ידוע.
- `source_type` — `repo-file`, `external-link`, `embed`, `mixed`, `unknown`.
- `source_url` — קישור מקור אם יש.
- `embed_url` — קישור הטמעה אם יש.
- `can_embed` — `true`, `false`, או `unknown`.
- `print_ready` — `true`, `false`, או `unknown`.
- `download_ready` — `true`, `false`, או `unknown`.
- `tags` — תגיות נוספות.
- `notes` — הערות אמיתיות בלבד.

---

## 7. סוגי משימות וקבצים מותרים

- `worksheet` — עבודה / דף עבודה.
- `summary-work` — עבודת סיכום / משימה מסכמת.
- `exam` — מבחן.
- `link` — קישור.
- `digital-task` — משימה מתוקשבת.
- `printable-task` — משימה להדפסה.
- `embedded-resource` — משאב להטמעה.
- `mixed` — משאב מעורב.
- `unknown` — לא ידוע.

כאשר יניב מוסיף סיווג חדש, חובה לעדכן גם את `RULES.md` וגם את `metadata/taxonomy.json`.

---

## 8. מדיניות הטמעה וקישור

- אם אפשר להטמיע קובץ/משימה באתר בצורה תקינה — מסמנים `can_embed=true` ומוסיפים `embed_url` אם צריך.
- אם אי אפשר להטמיע — מסמנים `can_embed=false` ומשתמשים בקישור.
- אם לא יודעים — מסמנים `can_embed=unknown`.
- אין להבטיח פעולה שהדפדפן לא מאפשר, כמו הדפסה שקטה בלי אישור משתמש.
- משימה להדפסה תסומן כך רק אם היא באמת מתאימה להדפסה.

---

## 9. כפילויות

כפילות נקבעת לפי תוכן ולא לפי שם.

- אם `content_hash` כבר קיים — אין להוסיף עותק נוסף.
- אם אותו קובץ מתאים לכמה כיתות/נושאים — מוסיפים שיוכים במטא־דאטה.
- אסור לשכפל קובץ כדי שיופיע בכמה קטגוריות באתר.

---

## 10. קבצי מקור מרכזיים

- `RULES.md` — מקור האמת המחייב.
- `AGENTS.md` — הוראות עבודה ל־AI/Codex.
- `metadata/index.json` — אינדקס הפריטים הפעילים.
- `metadata/taxonomy.json` — מילון סיווגים.
- `metadata/authors.json` — מילון מחברים.
- `metadata/site-structure.json` — מבנה ניווט ועיצוב לאתר העתידי.

---

## 11. מבנה תיקיות מומלץ

```text
files/
  middle-school/
    grade-7/
      algebra/
      geometry/
      summaries/
      exams/
      uncategorized/
    grade-8/
      algebra/
      geometry/
      summaries/
      exams/
      uncategorized/
    grade-9/
      algebra/
      geometry/
      summaries/
      exams/
      uncategorized/
  high-school/
    3-unit/
    4-unit/
    5-unit/
    unknown/
metadata/
review/
  pending_clarification/
```

התיקיות הן כלי עזר. הסינון האמיתי לאתר ולשליפה נעשה לפי המטא־דאטה.

---

## 12. מצב מאומת נוכחי

לפי `metadata/index.json`, קיימות כרגע ארבע רשומות פעילות באינדקס:

1. מאגר ז׳.
2. קובץ כיתה ח׳ בנושא יחס / פרופורציה / קנה מידה.
3. קובץ כיתה ח׳ של דפי סיכום בנושא יחס / פרופורציה / קנה מידה.
4. ריכוז שאלות דלתון לכיתה ט׳.

אין להציג קבצים אחרים כאילו הם קיימים אם הם לא באינדקס התקף.

---

## 13. סדר פעולה לכל AI / Codex

לפני כל פעולה בריפו חובה לקרוא:

1. `RULES.md`
2. `AGENTS.md`
3. `metadata/index.json`
4. `metadata/taxonomy.json`
5. `metadata/authors.json`
6. `metadata/site-structure.json`, אם קיים

בכל שינוי חובה לשמור על אמת, עברית RTL, מטא־דאטה עקבי, אי־שכפול קבצים והכנה לאתר עתידי גדול.

---

## 14. סתירות

אם יש סתירה בין קובץ ישן לבין `RULES.md`, הקובץ הזה גובר.
אם יש ספק — לא מנחשים.
