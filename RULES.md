# MAAGAR RULES — דף הכללים היחיד

Repository: `yanivmizrachiy/maagar`
Last updated: 2026-06-24

## 1. כלל עליון

`RULES.md` הוא דף הכללים היחיד והמחייב של הריפו.

אין לשמור כללים מחייבים ב־`AGENTS.md`, `STATE/`, `docs/`, הודעות צ׳אט, קומיטים או PR descriptions. אם יש כלל חדש, החלטת ארגון, מדיניות AI, מדיניות תצוגה, מדיניות סיווג או דרישת עבודה — היא נכנסת לכאן בלבד.

שאר הקבצים מותרים, אבל תפקידם מוגבל:
- `metadata/` — נתונים פעילים של המאגר.
- `AGENTS.md` — הפניה קצרה לסוכנים לקרוא את `RULES.md` ולעבוד לפי בדיקות הריפו.
- `STATE/` — דוחות מצב בלבד, לא כללים.
- `docs/` — מדריכים והסברים בלבד, לא כללים.
- `scripts/` — כלים אוטומטיים בלבד.

אם יש סתירה: `RULES.md` גובר. אם יש ספק: לא מנחשים.

## 2. מטרת הריפו

הריפו הוא מאגר אמיתי של חומרי מתמטיקה ואתר סטטי עברי להצגת החומרים לפי שכבה, תחום, נושא, סוג ורמה.

הריפו חייב לתמוך ב:
- שמירת קבצים אמיתיים בלבד.
- הצגת קבצים באתר GitHub Pages בעברית RTL.
- סיווג חומרים לפי metadata.
- חיפוש וסינון חכמים לפי שכבה, נושא, סוג, כותרת ותגיות.
- מניעת כפילויות לפי `content_hash`.
- התאמת קובץ אחד לכמה כיתות/נושאים/רמות בלי לשכפל אותו פיזית.

אסור ליצור דמו, קובץ פיקטיבי, כרטיסייה פיקטיבית, metadata מומצא או טענה שפיצ׳ר עובד אם לא אומת.

## 3. מצב אמת נוכחי

נכון ל־2026-06-24:

- ריפו: `yanivmizrachiy/maagar`.
- אתר חי: GitHub Pages.
- מבנה: סטטי, ללא backend.
- מקור אמת לקבצים: `metadata/index.json`.
- קבצים חיים באתר: 321.
- חלוקה מדווחת: ז׳ 104, ח׳ 134, ט׳ 80, חטיבה עליונה 3.
- האתר כולל מיון חכם, כפתורי מיון נוחים, מפתחות חיפוש/מיון מחושבים מראש, שמירת מצב מיון/סינון ב־URL, ניווט שכבה, ניווט יחידות חטיבה עליונה ותת־ניווט מבחנים.

אין להחזיר את הריפו למצב הישן של “4 קבצים”. כל מסמך ישן שטוען זאת נחשב לא מעודכן.

## 4. מבנה אתר מחייב

עמוד ראשי:
1. מתמטיקה לכיתה ז׳
2. מתמטיקה לכיתה ח׳
3. מתמטיקה לכיתה ט׳
4. חטיבה עליונה

כל כפתור שכבה מוביל למרכז שכבה ברור. לדוגמה: לחיצה על “מתמטיקה לכיתה ז׳” מציגה כותרת של כיתה ז׳ ומתחתיה כפתורים קצרים:
1. כל הקבצים
2. אלגברה
3. גיאומטריה
4. עבודות סיכום
5. מבחנים
6. חומרים שונים — רק אם יש קבצים לא מסווגים או unknown.

אין לכתוב בכל כפתור מחדש את שם השכבה כאשר הכותרת כבר מציינת את השכבה. הכפתורים צריכים להיות קצרים, ברורים ונוחים למורים.

בחטיבה עליונה:
1. 3 יחידות
2. 4 יחידות
3. 5 יחידות

חטיבה עליונה חייבת לעבוד לפי `unit_level` ב־metadata. כפתורי 3/4/5 יחידות הם חובה כאשר קיימים קבצים מתאימים. אין לשכפל קובץ בין יחידות; אם קובץ מתאים ליותר מרמת לימוד אחת, יש לייצג זאת ב־metadata ולא בעותקים פיזיים.

הסינון האמיתי נעשה לפי `metadata/index.json`; התיקיות הן כלי עזר בלבד.

## 5. ניווט יוקרתי ונוח למורים

האתר חייב להיראות כמו כלי עבודה יוקרתי ונוח למורים, לא כמו דף כפתורים רגיל שנוצר אוטומטית על ידי AI.

דרישות ניווט ועיצוב:
- כפתורי הניווט צריכים להיות עדינים, איכותיים, ברורים ונעימים לעין.
- ניווט לפי שכבה, יחידות, תחום, מבחנים, מיון וחיפוש חייב להופיע בהיררכיה ברורה.
- כל כפתור ניווט חייב להוביל לפעולה אמיתית באתר או לשנות סינון/מיון אמיתי.
- אין כפתורי דמו, אין כפתורי placeholder, ואין כפתורים שלא עושים פעולה אמיתית.
- הכפתורים צריכים להיות נוחים גם בטלפון וגם במחשב.
- העיצוב צריך להיות שימושי למורים: רגוע, מכוון פעולה, בלי אפקטים שמפריעים לקריאה.
- אזורי שכבה, יחידות, תחום ומבחנים צריכים להיות מובחנים ויזואלית בצורה עדינה.
- יש להשתמש בשכבת העיצוב `assets/site-premium-nav.css` עבור ניווט שכבה, תחומים, מבחנים ופרטי קובץ.
- יש להשתמש בשכבת העיצוב `assets/site-highschool-units.css` עבור ניווט 3/4/5 יחידות בחטיבה עליונה.

## 6. מבחנים ותת־ניווט מבחנים

כאשר לוחצים על “מבחנים” במרכז שכבה, חייב להיות תת־ניווט ברור:
1. כל המבחנים
2. מבחני סוף שנה
3. מבחני אמצע שנה
4. מבחני תחילת שנה
5. מבחני מיומנות

אם קיימים נתונים בכותרת, בשם הקובץ, בתגיות או בנושאים שמאפשרים לזהות סוג מבחן — יש לקטלג את המבחן לפי אחת מקטגוריות המבחנים.

אם לא ניתן לזהות בוודאות:
- לא מנחשים.
- משאירים תחת כל המבחנים.
- מסמנים unknown ב־metadata כשצריך.

## 7. פרטי קובץ בכרטיס ובצפייה

כל קובץ שמוצג באתר צריך להציג בצורה מעוצבת ונוחה את כל המידע הידוע עליו מתוך metadata, ככל שהוא קיים:
- מחבר
- שנה
- שכבות
- רמת יחידות כאשר מדובר בחטיבה עליונה
- תחום
- סוג קובץ
- מקור
- קטגוריית מבחן אם קיימת
- נושאים ותגיות רלוונטיים

אם פרט לא ידוע — לא ממציאים. מציגים רק מידע אמיתי או משאירים unknown במקור הנתונים.

## 8. הופעה בכמה שכבות וסיווג נכון

קובץ שייך לכל מקום אמיתי שמתאים לו לפי metadata.

אם קובץ מתאים גם לכיתה ז׳ וגם לכיתה ח׳:
- אין לשכפל את הקובץ פיזית.
- חובה לעדכן את `grades` כך שיכלול את שתי השכבות.
- הקובץ צריך להופיע גם במרכז כיתה ז׳ וגם במרכז כיתה ח׳.

אם קובץ מתאים לכמה נושאים:
- אין לשכפל את הקובץ פיזית.
- חובה לעדכן את `topics`.
- הקובץ צריך להופיע בכל סינון רלוונטי.

אם קובץ בחטיבה עליונה מתאים לכמה רמות יחידות:
- אין לשכפל את הקובץ פיזית.
- חובה לייצג את השיוך ב־metadata.
- אם אין דרך לייצג כמה יחידות בשדה יחיד, יש לשמור `unit_level=unknown` ולהוסיף תגיות/הערה אמיתיות עד להרחבת הסכימה.

אם יש חוסר ודאות לגבי שיוך קובץ:
- לא מנחשים.
- משתמשים ב־`unknown`.
- משאירים מקום לבדיקה עתידית.

## 9. מבנה תיקיות מחייב

```text
files/
  middle-school/
    grade-7/
      algebra/
      geometry/
      summaries/
      exams/
      uncategorized/
    grade-8/
      algebra/
      geometry/
      summaries/
      exams/
      uncategorized/
    grade-9/
      algebra/
      geometry/
      summaries/
      exams/
      uncategorized/
  high-school/
    3-unit/
    4-unit/
    5-unit/
    unknown/
metadata/
scripts/
STATE/
docs/
```

לא משכפלים קובץ כדי שיופיע בכמה מקומות. קובץ נשמר פיזית פעם אחת, ושיוך לכמה כיתות/נושאים/רמות נעשה דרך metadata.

## 10. שדות חובה ב־metadata

כל פריט ב־`metadata/index.json` חייב לכלול לפחות:

- `id`
- `title`
- `path` או `source_url`
- `file_name`
- `extension`
- `content_hash` כשזה קובץ פיזי
- `school_stage`
- `grade`
- `grades`
- `unit_level`
- `track`
- `primary_category`
- `topics`
- `document_type`
- `exam_kind`
- `bagrut_questionnaire`
- `year`
- `author`
- `source_type`
- `source_url`
- `embed_url`
- `can_embed`
- `print_ready`
- `download_ready`
- `tags`
- `notes`

אם פרט לא ידוע: לכתוב `unknown`. לא לנחש מחבר, שנה, נושא, רמה או מקור.

## 11. ערכים חוקיים מרכזיים

`school_stage`: `middle-school`, `high-school`, `unknown`.

`grade`: `7`, `8`, `9`, `high-school`, `unknown`.

`unit_level`: `3-unit`, `4-unit`, `5-unit`, `unknown`.

`primary_category`: `algebra`, `geometry`, `summaries`, `exams`, `uncategorized`, `unknown`.

`document_type`: `worksheet`, `summary-work`, `exam`, `link`, `digital-task`, `printable-task`, `embedded-resource`, `mixed`, `unknown`.

`exam_kind`: `start`, `mid`, `end`, `skill`, `bagrut`, `quiz`, `practice-test`, `unknown`.

`source_type`: `repo-file`, `external-link`, `embed`, `mixed`, `unknown`.

`can_embed`, `print_ready`, `download_ready`: `true`, `false`, או `unknown`.

אם מוסיפים ערך חדש — חובה לעדכן גם `metadata/taxonomy.json` וגם את הדף הזה.

## 12. סידור נושאים

המטרה: קבצים מאותו נושא יופיעו אחד ליד השני באתר וב־metadata.

כל קובץ צריך לקבל `topics` אמיתיים ככל האפשר. דוגמאות לנושאים קנוניים:

אלגברה:
- ביטויים אלגבריים
- הצבה
- משוואות
- משוואות בשני נעלמים
- מערכת משוואות
- פונקציה קווית
- פונקציה ריבועית
- משוואות ריבועיות
- יחס ופרופורציה
- אחוזים
- חוקיות וסדרות
- מערכת צירים
- מספרים מכוונים

גיאומטריה:
- זוויות
- משולשים
- חפיפת משולשים
- דמיון משולשים
- דלתון
- מלבן וריבוע
- מקבילית
- מעוין
- טרפז
- קטע אמצעים
- משפט פיתגורס
- גיאומטריה אנליטית

מבחנים וסיכומים:
- מבחן מיון והקבצה
- מבחן מחצית
- מבחן סוף שנה
- מבחן תחילת שנה
- מבחן אמצע שנה
- מבחן מיומנות
- קורס קיץ
- עבודת סיכום

כלי הסידור: `python3 scripts/topic-organizer.py`.
ברירת מחדל: dry-run בלבד.
שינוי אמיתי: `python3 scripts/topic-organizer.py --apply`.

אין להפעיל `--apply` בלי להריץ אחר כך:

```bash
bash scripts/validate-all.sh && python3 scripts/test-logic.py
```

## 13. כפילויות

כפילות נקבעת לפי תוכן, לא לפי שם.

- אם `content_hash` כבר קיים — לא מוסיפים עותק נוסף.
- אם אותו קובץ מתאים לכמה כיתות — מעדכנים `grades`.
- אם אותו קובץ מתאים לכמה נושאים — מעדכנים `topics`.
- אם אותו קובץ מתאים לכמה יחידות בחטיבה עליונה — לא משכפלים; מייצגים זאת ב־metadata או מסמנים unknown עד הרחבת הסכימה.
- אם יש קבצים עם אותו שם אבל hash שונה — לא מוחקים אוטומטית; מסמנים לבדיקה.

## 14. קבצים גדולים והטמעה

- קובצי PDF רגילים יכולים להיות ב־`files/`.
- קובץ מעל 10MB דורש תשומת לב.
- קובץ מעל 50MB לא מיועד להטמעה רגילה באתר.
- קובץ כבד יכול להיות `download_ready=true`, אבל `can_embed=false` ו־`print_ready=false` אם הוא לא מתאים לנייד.
- הדוגמה הקיימת: `חוברת.pdf` בגודל 97MB מסומן כבד מדי להטמעה.

אין להבטיח הדפסה שקטה, הורדה או הטמעה אם הדפדפן או הקובץ לא מאפשרים זאת.

## 15. עבודה עם AI / Codex / GPT / Claude

לפני כל שינוי בריפו:

```bash
git status
bash scripts/validate-all.sh
python3 scripts/test-logic.py
```

אחרי כל שינוי:

```bash
bash scripts/validate-all.sh && python3 scripts/test-logic.py
```

אם שינוי משפיע על UI:

```bash
node scripts/qa-browser.js
```

מותר ל־AI לבצע בלי לשאול:
- תיקוני metadata בטוחים.
- סידור נושאים לפי כותרת/נתיב קיימים.
- תיקון תיעוד לא מעודכן.
- הוספת בדיקות או כלי dry-run.
- PR/merge כאשר השינוי בטוח והבדיקות עוברות.

חובה לשאול לפני:
- מחיקת קובץ אמיתי.
- Force push.
- שינוי ארכיטקטורה גדול.
- שימוש בשירותים בתשלום.
- טיפול בסיסמאות/סודות.

## 16. סדר עדיפויות קבוע

1. אמת בלבד.
2. לא למחוק קבצים אמיתיים בלי אישור.
3. `RULES.md` הוא דף הכללים היחיד.
4. `metadata/index.json` הוא מקור האמת של הקבצים הפעילים.
5. קובץ פיזי פעם אחת; שיוכים דרך metadata.
6. נושאים דומים צריכים להיות מקובצים יחד.
7. כפתורי ניווט יוקרתיים, עדינים ונוחים למורים הם דרישה קבועה.
8. ניווט חטיבה עליונה לפי 3/4/5 יחידות דרך `unit_level` הוא דרישה קבועה.
9. כל שינוי חייב לעבור validation.
10. לא להשאיר סתירות תיעודיות שמטעות סוכני AI.

## 17. תיקונים אחרונים

### 2026-06-25 — תיקון תקיעת טעינת האתר (CI אדום) ותאימות בדיקות ל־Windows

1. **באג קריטי: לולאת `MutationObserver` אינסופית שהקפיאה את האתר.**
   - הקבצים `assets/site-view-share.js` ו־`assets/site-modal-share.js` הריצו
     `observer.observe(document.documentElement, { childList, subtree, attributes: true })`,
     וה־callback שלהם הציב `wa.href = ...` (שינוי attribute). הצבת ה־href הפעילה שוב
     את ה־observer → לולאה אינסופית שחסמה את ה־main thread, ולכן `load`/`DOMContentLoaded`
     לא נורו ו־`metadata/index.json` לא הסתיים להיטען.
   - תוצאה: בדיקת **Site Button Smoke Test** ב־GitHub Actions נכשלה (כל הבדיקות
     נתקעו ב־`Test timeout 120000ms`), והאתר עלול היה להיתקע גם אצל משתמשים.
   - תיקון: ה־observer צופה רק ב־`{ childList: true, subtree: true }` (בלי `attributes`).
     שינוי href אינו צריך להאזין לו. אומת בדפדפן אמיתי: `load` ב־251ms, 321 כרטיסים,
     כל הכפתורים והמודאל עובדים, 0 שגיאות.
   - כלל קבוע: אסור ש־callback של `MutationObserver` ישנה attribute שנמצא בתוך ה־subtree
     הנצפה כאשר `attributes: true` — זו לולאה אינסופית.

2. **תאימות סקריפטי בדיקה ל־Windows (`scripts/validate-all.sh`, `scripts/validate-index.sh`).**
   - הסקריפטים העבירו ל־Python נתיב בסגנון MSYS (`/c/Users/...`) ש־Python של Windows
     לא יכול לפתוח (FileNotFoundError), ובנוסף קראו JSON עברי בלי `encoding='utf-8'`
     (cp1252). ב־Linux/CI עברו; ב־Windows מקומי נכשלו.
   - תיקון: `cygpath -m` לנרמול הנתיב כשזמין, `encoding='utf-8'` בקריאות ה־JSON,
     וייצוא `PYTHONUTF8=1` / `PYTHONIOENCODING=utf-8`. אין השפעה על Linux/CI.
   - תוצאה: `bash scripts/validate-all.sh` עובר זהה ב־Windows וב־CI (47/0).

3. **הערה: `service-worker.js` אינו נרשם בשום מקום** (אין `navigator.serviceWorker.register`).
   זהו קוד מת ואינו משפיע על האתר. כל "כפתור ישן" שנראה באתר נובע מ־cache רגיל של הדפדפן,
   לא מ־service worker. לא להוסיף רישום SW בלי החלטה מפורשת (מחזיר בעיות cache).

### 2026-06-25 — הסרת כיתובי דמו/שיווק

לפי דרישת הבעלים: האתר הוא כלי, לא דף נחיתה. הוסרו כיתובי מילוי/שיווק (לא פונקציונליים),
תוך שמירה על המבנה (id/class/כותרות ניווט) כדי לא לשבור את `validate-site-shell.py`:
- `index.html`: באנר ה־hero ("כל הקבצים במקום אחד" + פסקת התיאור) ושורת התכונות תחת הלוגו
  ("צפייה מוטמעת · פתיחה · הורדה · שיתוף").
- `assets/site.js`: פסקאות התיאור השיווקיות ב־gradeGateway ("שער כניסה יוקרתי..."),
  ב־unit-hub ("בחר רמת לימוד. הסינון נעשה לפי unit_level...") וב־grade-hub
  ("בחר תחום. הכפתורים כאן קצרים..."). כותרת ה־gateway קוצרה ל"בחר שכבה".
- כלל קבוע: אין טקסט שיווקי/מילוי באתר. רק כותרות ניווט פונקציונליות, תוויות כפתורים,
  ספירות אמת ופרטי קובץ. אין placeholder גלוי ואין כיתובי דמו.

### 2026-06-25 — הסרת כפתור "עזרה מהירה" ולוח הסטטיסטיקה הכפול

לפי דרישת הבעלים:
- **כפתור/פאנל "עזרה מהירה"** הוסרו לגמרי (`site-help.js`: הוסרו `ensureHelpButton`,
  `ensureHelpPanel`, `openHelp/closeHelp`, קיצור המקלדת `?`, וסגנונות הפאנל). נשמרו שאר
  רכיבי הנגישות: אייקון, ARIA, skip-link, קיצור `/`, נרמול כפתורי פעולה. עודכנו בהתאם
  `tests/site-responsive.spec.js`, `tests/site-accessibility.spec.js` ו־`REQUIRED_HELP_SNIPPETS`.
- **לוח הסטטיסטיקה (`#stats`)** הוסר — הספירה "כמה קבצים / לצפייה / להורדה / נושאים" הופיעה
  פעמיים (גם בלוח וגם בשורת ה־meta). נשארה רק ספירת ההקשר בשורת ה־meta. הוסרו פונקציית
  `stats()`, מקטע ה־hero ב־`index.html`, ו־`stats` מ־`REQUIRED_IDS` ב־validator.
- כלל קבוע: אין כפילות מידע באתר. כל נתון מוצג פעם אחת בלבד.

### 2026-06-25 — הסרת כותרות מקטעים מיותרות

לפי דרישת הבעלים, הוסרו כותרות ה־`<h2>` של המקטעים ב־`assets/site.js` (כרטיסי השכבות
וה־chips מספיקים, אין צורך בכותרת נוספת מעליהם):
- gradeGateway: "בחר שכבה" (והוסר עוטף `grade-gateway-head`).
- grade-hub: "מתמטיקה ל<שכבה>".
- unit-hub: "חטיבה עליונה לפי יחידות".
המבנה נשמר (`grade-gateway`, `grade-entry-grid`, `unit-hub`, `grade-hub`, ה־chips);
ה־validator בודק את ה־classes מול ה־CSS/`site-structure.json`, לא מול ה־JS, ולכן נשאר ירוק.
- כלל קבוע: כותרות מקטע רק כשהן מוסיפות מידע. אין כותרת שחוזרת על מה שהכרטיסים/הכפתורים כבר מראים.

### 2026-06-25 — שיפור עריכוּת (ריכוז טקסטים + מדריך מדויק)

כדי שעריכות עתידיות יהיו קלות ובטוחות:
- **כל הטקסטים מרוכזים בראש `assets/site.js`** — מילוני התוויות הקיימים (`G`, `C`, `T`, `U`,
  `SORTS`, `DOMAIN_BUTTONS`, `EXAM_BUCKETS`, `GATEWAY_HINTS`) + object חדש `UI` שמרכז את כל
  הכיתובים הבודדים שהיו מפוזרים בפונקציות (כותרות, "כל המאגר", מצב ריק, הודעות, "מחבר:"/"שנה:").
  יש באנר הערה שמסמן את האזור. הלוגיקה למטה לא נגעה. אומת: 0 שגיאות, כל הכיתובים מרונדרים.
- **ההפרדה כבר מלאה**: HTML ב-`index.html`, כל ה-CSS ב-`assets/*.css` (צבעים ב-`:root` של
  `site.css`), לוגיקה ב-`assets/*.js`. אין CSS/JS בתוך `index.html`.
- **`docs/EDITING_GUIDE.md` נכתב מחדש** — הגרסה הקודמת תיארה ארכיטקטורה ישנה שלא קיימת
  (`<style>`/`<script>` בתוך index.html, `GRADE_IDENTITY`, `renderFileCard`...) והייתה מטעה.
  עכשיו הוא מפה מדויקת: "מה רוצים לשנות → איזה קובץ → איפה".
- כלל קבוע: טקסט באתר נערך רק בבלוק העליון של `site.js`; צבעים רק ב-`site.css`. אין כיתוב קשיח
  מפוזר בלוגיקה.

### 2026-06-25 — הסרת כל תיאורי הדמו מכרטיסי השכבה

לפי דרישת הבעלים (סריקה יסודית של כל כיתוב תיאורי/מילוי):
- הוסר `GATEWAY_HINTS` לגמרי (כל ארבעת התיאורים: "הכנה מסודרת...", "אלגברה, גיאומטריה...",
  "חומרים מסודרים...", "כניסה לפי יחידות...") — היו טקסט מילוי תחת כרטיסי השכבה.
- הוסרו מכרטיס השכבה גם הקיקר "כניסה לשכבה", ה־fallback "חומרים מסודרים למורים",
  והכיתוב "מבחנים לפי סינון". הכרטיס מציג כעת רק את שם השכבה + ספירת קבצים אמיתית.
- עודכן `REQUIRED_JS_SNIPPETS` (הוסר `const GATEWAY_HINTS`).
- כלל קבוע: בכרטיסי הניווט אין טקסט תיאורי/שיווקי — רק שם פונקציונלי וספירת אמת.
  (נשמר "מיון: שכבה › תחום › נושא" כי הוא אינדיקציה פונקציונלית של סדר המיון, לא דמו.)

### 2026-06-25 — סגנון כפתורי השכבות: "זוהר בהיר" (צבעים קבועים, ללא בורר)

הבעלים בחר (מתוך 15 הצעות) את סגנון **"זוהר בהיר"** לכפתורי עמוד הכניסה: מילוי בהיר וחי +
הילה רכה באותו צבע (`box-shadow:0 0 24px <color>80`) + טקסט כהה קריא.
- **בקשת הבעלים: אין בורר צבעים באתר — הצבעים קבועים.** הוסר לגמרי בורר ה־palette
  (ה־`#palette` ב־index.html, `initPalette()` ו־localStorage ב־site.js, וסגנונות ה־`.sw` ב־CSS).
- הצבעים הקבועים ב־`site-premium-nav.css` (ללא `data-palette`):
  כיתה 1=`#facc15` (צהוב), 2=`#22d3ee` (ציאן), 3=`#f472b6` (ורוד), 4=`#4ade80` (ירוק),
  כל אחד עם הילה באותו צבע ו־`.grade-entry strong/em` כהה (`#0b1220`/`#1b2435`).
- כלל קבוע: צבעי כפתורי השכבות קבועים בקוד; אין בחירת צבעים בזמן ריצה.

### 2026-06-25 — הסרת שיתוף "תצוגה" + ניקוי קוד מת

לפי בקשת הבעלים (פחות עומס, ריפו נקי):
- **הוסר לגמרי `assets/site-view-share.js`** — כפתורי "🔗 העתק תצוגה" / "🟢 שתף תצוגה" שבכותרת
  (שיתוף הקישור לתצוגה המסוננת). הוסרו כל ההפניות: טעינת הסקריפט ב־index.html, סלקטור ב־site.css,
  ה־feature `current_view_share_buttons` בדוחות (`site-action-report.py` ועוד), הדרישות ב־
  `validate-site-shell.py`/`validate-all.sh`/`validate-real-buttons.py`, ובדיקות `site-buttons`/`site-responsive`.
- **נמחק `service-worker.js`** — היה קוד מת (לא נרשם ולא נטען בשום מקום).
- **נשמר** שיתוף קובץ בודד (`site-share.js` בכרטיס + `site-modal-share.js` בחלון הצפייה):
  "העתק קישור" + "WhatsApp" לקובץ הספציפי — זה פונקציונלי ושימושי.
- כלל קבוע: אין שיתוף "תצוגה/חיפוש"; שיתוף הוא ברמת הקובץ בלבד.

### 2026-06-28 — ניווט drill-down לפי נושא (4 רמות)

לפי דרישת הבעלים, הניווט העמיק לרמת נושא:
- **בית** (בחירת שכבה) → **שכבה** (כפתורי תחום: אלגברה/גיאומטריה/עבודות סיכום/מבחנים) →
  **תחום** (רשימת הנושאים באותו תחום, למשל משוואות/פיתגורס) → **נושא** (הקבצים/המשימות).
- בכל שלב מוצג רק הרלוונטי + כפתור "חזרה" שעולה רמה אחת. אין רשימת קבצים עד בחירת נושא
  (אלא אם לתחום אין נושאים מסווגים — אז הקבצים מוצגים ישירות).
- מומש ב־`assets/site.js`: מצב `S.topic`, `topicsForCurrent()`, `topicHub()`, `topicButton()`,
  `backButton()` עם `data-back`, ו־`filters()`/`files()` מודעי-שלב. הוסר הכפתור "כל הקבצים"
  מרמת התחומים (הדריל הוא לפי נושא). בדיקות `site-responsive`/`site-accessibility` עודכנו לזרימה.
- **הערה תשתית**: `C:\Users\yaniv\Desktop\maagar` הוא כעת symlink ל־`C:\YANIV\Projects\Active\maagar`
  (הריפו הפיזי). אותו remote (yanivmizrachiy/maagar). דורש `git safe.directory` לנתיב האמיתי.

### 2026-06-28 — היפוך העיצוב לתמה בהירה (רקע לבן, טקסט כהה)

לפי דרישת הבעלים ("רקע לבן עם כפתורים בכיתוב כהה"): כל 3 קבצי ה־CSS הומרו לתמה בהירה —
- `site.css`: `:root` בהיר (`--bg` בהיר, `--text:#0f1b2e`, `--line` כהה־שקוף, משטחים לבנים),
  רקע גוף בהיר, כותרת/חיפוש/כרטיסים/מודאל/צ'יפים לבנים עם טקסט כהה וצללים רכים.
- `site-premium-nav.css`: ה־hubs/gateway/chips לבנים עם טקסט כהה. **כפתורי השכבות נשארו
  "זוהר בהיר"** (צהוב/ציאן/ורוד/ירוק, טקסט כהה) — בולטים יפה על הרקע הלבן. נוסף `.topic-chip`.
- `site-highschool-units.css`: unit-hub/chips בהירים.
- `index.html`: `theme-color=#eef1f7`, `color-scheme=light`.
- כלל קבוע: התמה בהירה — רקע לבן/בהיר, טקסט כהה (`#0f1b2e`/`--muted`), אקצנטים צבעוניים.
  כפתורי הפעולה בהירים עם טקסט כהה; צ'יפ נבחר (`.on`) מקבל גרדיאנט צבעוני עם טקסט לבן.

### 2026-06-25 — ניווט drill-down, כרטיסים נקיים, ובורר צבעים

לפי דרישת הבעלים, שינוי מבני בעמוד הראשי ובכרטיסים:
- **עמוד הבית = רק 4 כפתורי השכבות** (מתמטיקה לכיתה ז׳/ח׳/ט׳/חטיבה עליונה). הוסרו לגמרי
  כפל הכפתורים (chips של שכבות שכפלו את הכרטיסים) וכפתורי הקטגוריה/סוג. אין רשימת קבצים בבית —
  רק רמז "בחרו שכבה".
- **כניסה לשכבה (drill-down)**: לחיצה על שכבה פותחת עמוד עם כפתור "חזרה", כפתורי נושאים
  (אלגברה/גיאומטריה/עבודות סיכום/מבחנים), מיון, ורשימת הקבצים של אותה שכבה.
- **כרטיס קובץ מינימלי**: רק אייקון + כותרת + תגיות נושא + כפתורי פעולה. הוסרו תגיות שכבה/תחום/סוג,
  בלוק `file-details` (לא נקרא), שורת ה־meta ("📁 קובץ פנימי · הורדה ישירה"), וכותרת המשנה
  "· הורדה ישירה זמינה" בחלון הצפייה. בקבצים אין שום כיתוב מיותר.
- **4 צבעים שונים מאוד לכפתורי השכבות + בורר**: 3 ערכות צבעים (`body[data-palette]` a/b/c)
  ב־`site-premium-nav.css`; בורר swatches בכותרת (`#palette`), נשמר ב־localStorage
  (`maagar-palette`), אותחל ב־`initPalette()`. בכל ערכה 4 הצבעים שונים מאוד זה מזה.
- עודכנו הבדיקות (`site-responsive`/`site-accessibility`) לזרימת ה־drill-down, והוסר snippet
  "הורדה ישירה זמינה" מ־`validate-real-buttons.py` (ההורדה מאומתת עדיין דרך "הורדה מהירה"/fast-download).
- כלל קבוע: עמוד הבית = בחירת שכבה בלבד; הנושאים בתוך השכבה; הכרטיס מציג רק מה שמזהה אותו וכפתורים.
