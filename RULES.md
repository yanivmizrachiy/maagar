# MAAGAR RULES — דף הכללים היחיד

Repository: `yanivmizrachiy/maagar`
Last updated: 2026-06-24

## 1. כלל עליון

`RULES.md` הוא דף הכללים היחיד והמחייב של הריפו.

אין לשמור כללים מחייבים ב־`AGENTS.md`, `STATE/`, `docs/`, הודעות צ׳אט, קומיטים או PR descriptions. אם יש כלל חדש, החלטת ארגון, מדיניות AI, מדיניות תצוגה, מדיניות סיווג או דרישת עבודה — היא נכנסת לכאן בלבד.

שאר הקבצים מותרים, אבל תפקידם מוגבל:
- `metadata/` — נתונים פעילים של המאגר.
- `AGENTS.md` — הפניה קצרה לסוכנים לקרוא את `RULES.md` ולעבוד לפי בדיקות הריפו.
- `STATE/` — דוחות מצב בלבד, לא כללים.
- `docs/` — מדריכים והסברים בלבד, לא כללים.
- `scripts/` — כלים אוטומטיים בלבד.

אם יש סתירה: `RULES.md` גובר. אם יש ספק: לא מנחשים.

## 2. מטרת הריפו

הריפו הוא מאגר אמיתי של חומרי מתמטיקה ואתר סטטי עברי להצגת החומרים לפי שכבה, תחום, נושא, סוג ורמה.

הריפו חייב לתמוך ב:
- שמירת קבצים אמיתיים בלבד.
- הצגת קבצים באתר GitHub Pages בעברית RTL.
- סיווג חומרים לפי metadata.
- חיפוש וסינון חכמים לפי שכבה, נושא, סוג, כותרת ותגיות.
- מניעת כפילויות לפי `content_hash`.
- התאמת קובץ אחד לכמה כיתות/נושאים/רמות בלי לשכפל אותו פיזית.

אסור ליצור דמו, קובץ פיקטיבי, כרטיסייה פיקטיבית, metadata מומצא או טענה שפיצ׳ר עובד אם לא אומת.

## 3. מצב אמת נוכחי

נכון ל־2026-06-24:

- ריפו: `yanivmizrachiy/maagar`.
- אתר חי: GitHub Pages.
- מבנה: סטטי, ללא backend.
- מקור אמת לקבצים: `metadata/index.json`.
- קבצים חיים באתר: 321.
- חלוקה מדווחת: ז׳ 104, ח׳ 134, ט׳ 80, חטיבה עליונה 3.
- האתר כולל מיון חכם, כפתורי מיון נוחים, מפתחות חיפוש/מיון מחושבים מראש, שמירת מצב מיון/סינון ב־URL, ניווט שכבה, ניווט יחידות חטיבה עליונה ותת־ניווט מבחנים.

אין להחזיר את הריפו למצב הישן של “4 קבצים”. כל מסמך ישן שטוען זאת נחשב לא מעודכן.

## 4. מבנה אתר מחייב

עמוד ראשי:
1. מתמטיקה לכיתה ז׳
2. מתמטיקה לכיתה ח׳
3. מתמטיקה לכיתה ט׳
4. חטיבה עליונה

כל כפתור שכבה מוביל למרכז שכבה ברור. לדוגמה: לחיצה על “מתמטיקה לכיתה ז׳” מציגה כותרת של כיתה ז׳ ומתחתיה כפתורים קצרים:
1. כל הקבצים
2. אלגברה
3. גיאומטריה
4. עבודות סיכום
5. מבחנים
6. חומרים שונים — רק אם יש קבצים לא מסווגים או unknown.

אין לכתוב בכל כפתור מחדש את שם השכבה כאשר הכותרת כבר מציינת את השכבה. הכפתורים צריכים להיות קצרים, ברורים ונוחים למורים.

בחטיבה עליונה:
1. 3 יחידות
2. 4 יחידות
3. 5 יחידות

חטיבה עליונה חייבת לעבוד לפי `unit_level` ב־metadata. כפתורי 3/4/5 יחידות הם חובה כאשר קיימים קבצים מתאימים. אין לשכפל קובץ בין יחידות; אם קובץ מתאים ליותר מרמת לימוד אחת, יש לייצג זאת ב־metadata ולא בעותקים פיזיים.

הסינון האמיתי נעשה לפי `metadata/index.json`; התיקיות הן כלי עזר בלבד.

## 5. ניווט יוקרתי ונוח למורים

האתר חייב להיראות כמו כלי עבודה יוקרתי ונוח למורים, לא כמו דף כפתורים רגיל שנוצר אוטומטית על ידי AI.

דרישות ניווט ועיצוב:
- כפתורי הניווט צריכים להיות עדינים, איכותיים, ברורים ונעימים לעין.
- ניווט לפי שכבה, יחידות, תחום, מבחנים, מיון וחיפוש חייב להופיע בהיררכיה ברורה.
- כל כפתור ניווט חייב להוביל לפעולה אמיתית באתר או לשנות סינון/מיון אמיתי.
- אין כפתורי דמו, אין כפתורי placeholder, ואין כפתורים שלא עושים פעולה אמיתית.
- הכפתורים צריכים להיות נוחים גם בטלפון וגם במחשב.
- העיצוב צריך להיות שימושי למורים: רגוע, מכוון פעולה, בלי אפקטים שמפריעים לקריאה.
- אזורי שכבה, יחידות, תחום ומבחנים צריכים להיות מובחנים ויזואלית בצורה עדינה.
- יש להשתמש בשכבת העיצוב `assets/site-premium-nav.css` עבור ניווט שכבה, תחומים, מבחנים ופרטי קובץ.
- יש להשתמש בשכבת העיצוב `assets/site-highschool-units.css` עבור ניווט 3/4/5 יחידות בחטיבה עליונה.

## 6. מבחנים ותת־ניווט מבחנים

כאשר לוחצים על “מבחנים” במרכז שכבה, חייב להיות תת־ניווט ברור:
1. כל המבחנים
2. מבחני סוף שנה
3. מבחני אמצע שנה
4. מבחני תחילת שנה
5. מבחני מיומנות

אם קיימים נתונים בכותרת, בשם הקובץ, בתגיות או בנושאים שמאפשרים לזהות סוג מבחן — יש לקטלג את המבחן לפי אחת מקטגוריות המבחנים.

אם לא ניתן לזהות בוודאות:
- לא מנחשים.
- משאירים תחת כל המבחנים.
- מסמנים unknown ב־metadata כשצריך.

## 7. פרטי קובץ בכרטיס ובצפייה

כל קובץ שמוצג באתר צריך להציג בצורה מעוצבת ונוחה את כל המידע הידוע עליו מתוך metadata, ככל שהוא קיים:
- מחבר
- שנה
- שכבות
- רמת יחידות כאשר מדובר בחטיבה עליונה
- תחום
- סוג קובץ
- מקור
- קטגוריית מבחן אם קיימת
- נושאים ותגיות רלוונטיים

אם פרט לא ידוע — לא ממציאים. מציגים רק מידע אמיתי או משאירים unknown במקור הנתונים.

## 8. הופעה בכמה שכבות וסיווג נכון

קובץ שייך לכל מקום אמיתי שמתאים לו לפי metadata.

אם קובץ מתאים גם לכיתה ז׳ וגם לכיתה ח׳:
- אין לשכפל את הקובץ פיזית.
- חובה לעדכן את `grades` כך שיכלול את שתי השכבות.
- הקובץ צריך להופיע גם במרכז כיתה ז׳ וגם במרכז כיתה ח׳.

אם קובץ מתאים לכמה נושאים:
- אין לשכפל את הקובץ פיזית.
- חובה לעדכן את `topics`.
- הקובץ צריך להופיע בכל סינון רלוונטי.

אם קובץ בחטיבה עליונה מתאים לכמה רמות יחידות:
- אין לשכפל את הקובץ פיזית.
- חובה לייצג את השיוך ב־metadata.
- אם אין דרך לייצג כמה יחידות בשדה יחיד, יש לשמור `unit_level=unknown` ולהוסיף תגיות/הערה אמיתיות עד להרחבת הסכימה.

אם יש חוסר ודאות לגבי שיוך קובץ:
- לא מנחשים.
- משתמשים ב־`unknown`.
- משאירים מקום לבדיקה עתידית.

## 9. מבנה תיקיות מחייב

```text
files/
  middle-school/
    grade-7/
      algebra/
      geometry/
      summaries/
      exams/
      uncategorized/
    grade-8/
      algebra/
      geometry/
      summaries/
      exams/
      uncategorized/
    grade-9/
      algebra/
      geometry/
      summaries/
      exams/
      uncategorized/
  high-school/
    3-unit/
    4-unit/
    5-unit/
    unknown/
metadata/
scripts/
STATE/
docs/
```

לא משכפלים קובץ כדי שיופיע בכמה מקומות. קובץ נשמר פיזית פעם אחת, ושיוך לכמה כיתות/נושאים/רמות נעשה דרך metadata.

## 10. שדות חובה ב־metadata

כל פריט ב־`metadata/index.json` חייב לכלול לפחות:

- `id`
- `title`
- `path` או `source_url`
- `file_name`
- `extension`
- `content_hash` כשזה קובץ פיזי
- `school_stage`
- `grade`
- `grades`
- `unit_level`
- `track`
- `primary_category`
- `topics`
- `document_type`
- `exam_kind`
- `bagrut_questionnaire`
- `year`
- `author`
- `source_type`
- `source_url`
- `embed_url`
- `can_embed`
- `print_ready`
- `download_ready`
- `tags`
- `notes`

אם פרט לא ידוע: לכתוב `unknown`. לא לנחש מחבר, שנה, נושא, רמה או מקור.

## 11. ערכים חוקיים מרכזיים

`school_stage`: `middle-school`, `high-school`, `unknown`.

`grade`: `7`, `8`, `9`, `high-school`, `unknown`.

`unit_level`: `3-unit`, `4-unit`, `5-unit`, `unknown`.

`primary_category`: `algebra`, `geometry`, `summaries`, `exams`, `uncategorized`, `unknown`.

`document_type`: `worksheet`, `summary-work`, `exam`, `link`, `digital-task`, `printable-task`, `embedded-resource`, `mixed`, `unknown`.

`exam_kind`: `start`, `mid`, `end`, `skill`, `bagrut`, `quiz`, `practice-test`, `unknown`.

`source_type`: `repo-file`, `external-link`, `embed`, `mixed`, `unknown`.

`can_embed`, `print_ready`, `download_ready`: `true`, `false`, או `unknown`.

אם מוסיפים ערך חדש — חובה לעדכן גם `metadata/taxonomy.json` וגם את הדף הזה.

## 12. סידור נושאים

המטרה: קבצים מאותו נושא יופיעו אחד ליד השני באתר וב־metadata.

כל קובץ צריך לקבל `topics` אמיתיים ככל האפשר. דוגמאות לנושאים קנוניים:

אלגברה:
- ביטויים אלגבריים
- הצבה
- משוואות
- משוואות בשני נעלמים
- מערכת משוואות
- פונקציה קווית
- פונקציה ריבועית
- משוואות ריבועיות
- יחס ופרופורציה
- אחוזים
- חוקיות וסדרות
- מערכת צירים
- מספרים מכוונים

גיאומטריה:
- זוויות
- משולשים
- חפיפת משולשים
- דמיון משולשים
- דלתון
- מלבן וריבוע
- מקבילית
- מעוין
- טרפז
- קטע אמצעים
- משפט פיתגורס
- גיאומטריה אנליטית

מבחנים וסיכומים:
- מבחן מיון והקבצה
- מבחן מחצית
- מבחן סוף שנה
- מבחן תחילת שנה
- מבחן אמצע שנה
- מבחן מיומנות
- קורס קיץ
- עבודת סיכום

כלי הסידור: `python3 scripts/topic-organizer.py`.
ברירת מחדל: dry-run בלבד.
שינוי אמיתי: `python3 scripts/topic-organizer.py --apply`.

אין להפעיל `--apply` בלי להריץ אחר כך:

```bash
bash scripts/validate-all.sh && python3 scripts/test-logic.py
```

## 13. כפילויות

כפילות נקבעת לפי תוכן, לא לפי שם.

- אם `content_hash` כבר קיים — לא מוסיפים עותק נוסף.
- אם אותו קובץ מתאים לכמה כיתות — מעדכנים `grades`.
- אם אותו קובץ מתאים לכמה נושאים — מעדכנים `topics`.
- אם אותו קובץ מתאים לכמה יחידות בחטיבה עליונה — לא משכפלים; מייצגים זאת ב־metadata או מסמנים unknown עד הרחבת הסכימה.
- אם יש קבצים עם אותו שם אבל hash שונה — לא מוחקים אוטומטית; מסמנים לבדיקה.

## 14. קבצים גדולים והטמעה

- קובצי PDF רגילים יכולים להיות ב־`files/`.
- קובץ מעל 10MB דורש תשומת לב.
- קובץ מעל 50MB לא מיועד להטמעה רגילה באתר.
- קובץ כבד יכול להיות `download_ready=true`, אבל `can_embed=false` ו־`print_ready=false` אם הוא לא מתאים לנייד.
- הדוגמה הקיימת: `חוברת.pdf` בגודל 97MB מסומן כבד מדי להטמעה.

אין להבטיח הדפסה שקטה, הורדה או הטמעה אם הדפדפן או הקובץ לא מאפשרים זאת.

## 15. עבודה עם AI / Codex / GPT / Claude

לפני כל שינוי בריפו:

```bash
git status
bash scripts/validate-all.sh
python3 scripts/test-logic.py
```

אחרי כל שינוי:

```bash
bash scripts/validate-all.sh && python3 scripts/test-logic.py
```

אם שינוי משפיע על UI:

```bash
node scripts/qa-browser.js
```

מותר ל־AI לבצע בלי לשאול:
- תיקוני metadata בטוחים.
- סידור נושאים לפי כותרת/נתיב קיימים.
- תיקון תיעוד לא מעודכן.
- הוספת בדיקות או כלי dry-run.
- PR/merge כאשר השינוי בטוח והבדיקות עוברות.

חובה לשאול לפני:
- מחיקת קובץ אמיתי.
- Force push.
- שינוי ארכיטקטורה גדול.
- שימוש בשירותים בתשלום.
- טיפול בסיסמאות/סודות.

## 16. סדר עדיפויות קבוע

1. אמת בלבד.
2. לא למחוק קבצים אמיתיים בלי אישור.
3. `RULES.md` הוא דף הכללים היחיד.
4. `metadata/index.json` הוא מקור האמת של הקבצים הפעילים.
5. קובץ פיזי פעם אחת; שיוכים דרך metadata.
6. נושאים דומים צריכים להיות מקובצים יחד.
7. כפתורי ניווט יוקרתיים, עדינים ונוחים למורים הם דרישה קבועה.
8. ניווט חטיבה עליונה לפי 3/4/5 יחידות דרך `unit_level` הוא דרישה קבועה.
9. כל שינוי חייב לעבור validation.
10. לא להשאיר סתירות תיעודיות שמטעות סוכני AI.

## 17. תיקונים אחרונים

### 2026-06-25 — תיקון תקיעת טעינת האתר (CI אדום) ותאימות בדיקות ל־Windows

1. **באג קריטי: לולאת `MutationObserver` אינסופית שהקפיאה את האתר.**
   - הקבצים `assets/site-view-share.js` ו־`assets/site-modal-share.js` הריצו
     `observer.observe(document.documentElement, { childList, subtree, attributes: true })`,
     וה־callback שלהם הציב `wa.href = ...` (שינוי attribute). הצבת ה־href הפעילה שוב
     את ה־observer → לולאה אינסופית שחסמה את ה־main thread, ולכן `load`/`DOMContentLoaded`
     לא נורו ו־`metadata/index.json` לא הסתיים להיטען.
   - תוצאה: בדיקת **Site Button Smoke Test** ב־GitHub Actions נכשלה (כל הבדיקות
     נתקעו ב־`Test timeout 120000ms`), והאתר עלול היה להיתקע גם אצל משתמשים.
   - תיקון: ה־observer צופה רק ב־`{ childList: true, subtree: true }` (בלי `attributes`).
     שינוי href אינו צריך להאזין לו. אומת בדפדפן אמיתי: `load` ב־251ms, 321 כרטיסים,
     כל הכפתורים והמודאל עובדים, 0 שגיאות.
   - כלל קבוע: אסור ש־callback של `MutationObserver` ישנה attribute שנמצא בתוך ה־subtree
     הנצפה כאשר `attributes: true` — זו לולאה אינסופית.

2. **תאימות סקריפטי בדיקה ל־Windows (`scripts/validate-all.sh`, `scripts/validate-index.sh`).**
   - הסקריפטים העבירו ל־Python נתיב בסגנון MSYS (`/c/Users/...`) ש־Python של Windows
     לא יכול לפתוח (FileNotFoundError), ובנוסף קראו JSON עברי בלי `encoding='utf-8'`
     (cp1252). ב־Linux/CI עברו; ב־Windows מקומי נכשלו.
   - תיקון: `cygpath -m` לנרמול הנתיב כשזמין, `encoding='utf-8'` בקריאות ה־JSON,
     וייצוא `PYTHONUTF8=1` / `PYTHONIOENCODING=utf-8`. אין השפעה על Linux/CI.
   - תוצאה: `bash scripts/validate-all.sh` עובר זהה ב־Windows וב־CI (47/0).

3. **הערה: `service-worker.js` אינו נרשם בשום מקום** (אין `navigator.serviceWorker.register`).
   זהו קוד מת ואינו משפיע על האתר. כל "כפתור ישן" שנראה באתר נובע מ־cache רגיל של הדפדפן,
   לא מ־service worker. לא להוסיף רישום SW בלי החלטה מפורשת (מחזיר בעיות cache).
