# דוח מצב — maagar

Repository: `yanivmizrachiy/maagar`
Updated: 2026-06-23

דוח זה הוא דוח מצב בלבד. הוא אינו דף כללים. הכללים המחייבים היחידים נמצאים ב־`RULES.md`.

## מצב נוכחי

- הריפו ציבורי ופעיל.
- האתר פעיל כ־GitHub Pages סטטי בעברית RTL.
- אין backend.
- מקור האמת של הקבצים הפעילים: `metadata/index.json`.
- מקור האמת של הכללים: `RULES.md` בלבד.
- מספר קבצים חיים באתר: 321.
- חלוקה מדווחת: ז׳ 104, ח׳ 134, ט׳ 80, חטיבה עליונה 3.

## קבצי מפתח

- `RULES.md` — דף הכללים היחיד.
- `metadata/index.json` — אינדקס הקבצים הפעילים.
- `metadata/taxonomy.json` — ערכי סיווג חוקיים.
- `metadata/site-structure.json` — מבנה ניווט ותצוגה.
- `index.html` — אתר סטטי עברי RTL.
- `scripts/validate-all.sh` — בדיקת תקינות מלאה.
- `scripts/test-logic.py` — בדיקת ניווט וקבצים יתומים.
- `scripts/topic-organizer.py` — כלי סידור נושאים וקיבוץ קבצים דומים.

## היסטוריית עבודה חשובה

- PR #13: קליטת מאגר מתמטיקה גדול — 321 קבצים חיים באתר.
- PR #15: הוספת כלי סידור נושאים ל־metadata.
- cleanup-rules: איחוד הכללים ל־`RULES.md` בלבד וניקוי מסמכי מצב/AI כדי שלא יהיו מקורות כללים נוספים.

## מצב ארגון נושאים

השלב הראשון בוצע: נוצר כלי ייעודי לסידור נושאים.

הכלי יודע לזהות ולקבץ נושאים כגון:

- משוואות בשני נעלמים
- משוואות
- פונקציה קווית
- פונקציה ריבועית
- משוואות ריבועיות
- יחס ופרופורציה
- אחוזים
- מספרים מכוונים
- זוויות
- משולשים
- דלתון
- מלבן וריבוע
- מקבילית
- מעוין
- טרפז
- מבחני מיון והקבצה
- מבחני מחצית
- מבחני סוף שנה

פקודת בדיקה יבשה:

```bash
python3 scripts/topic-organizer.py --report reports/topic-audit.json
```

פקודת יישום אמיתית:

```bash
python3 scripts/topic-organizer.py --apply
bash scripts/validate-all.sh && python3 scripts/test-logic.py
```

## בעיות שעדיין דורשות המשך עבודה

- להריץ בפועל את `topic-organizer.py --apply` בסביבה מקומית/Claude/Codex ולבדוק את תוצאות הסידור.
- לשפר ידנית `topics` לקבצים שיישארו עם `unknown`.
- לנקות כותרות תצוגה לא יפות, בלי לשנות קבצים פיזיים אם אין צורך.
- לזהות קבצים שנראים כפולים בשם אבל שונים ב־`content_hash`.
- לסמן קבצים גדולים כ־`large-file` או להשאירם להורדה בלבד.

## פקודות אימות

```bash
bash scripts/validate-all.sh
python3 scripts/test-logic.py
node scripts/qa-browser.js
```

## הערה חשובה

אין למחוק קבצים אמיתיים ללא אישור מפורש של יניב. הסדר בשלב זה הוא metadata, נושאים, תיעוד, וחיפוש — לא מחיקה.
