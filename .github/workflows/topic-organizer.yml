name: Topic Organizer

on:
  workflow_dispatch:
    inputs:
      apply:
        description: "Apply metadata topics and clean display titles"
        required: true
        default: "false"
        type: choice
        options:
          - "false"
          - "true"

permissions:
  contents: write

jobs:
  topic-organizer:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Validate topic map
        run: |
          python3 scripts/validate-topic-map.py

      - name: Validate standalone site shell
        run: |
          python3 scripts/validate-site-shell.py
          node --check assets/site.js

      - name: Run audits
        run: |
          mkdir -p reports
          python3 scripts/topic-organizer.py --report reports/topic-audit.json
          python3 scripts/metadata-audit.py --report reports/metadata-audit.json
          python3 scripts/title-cleaner.py --report reports/title-cleanup.json
          python3 scripts/smart-topic-review.py --report reports/smart-topic-review.json
          python3 scripts/smart-cleanup-plan.py --json reports/smart-cleanup-plan.json --md reports/smart-cleanup-plan.md

      - name: Apply metadata cleanup
        if: ${{ github.event.inputs.apply == 'true' }}
        run: |
          python3 scripts/topic-organizer.py --apply --report reports/topic-audit.json
          python3 scripts/title-cleaner.py --apply --report reports/title-cleanup.json
          python3 scripts/sync-rules-tools.py
          python3 scripts/metadata-audit.py --report reports/metadata-audit.json
          python3 scripts/smart-topic-review.py --report reports/smart-topic-review.json
          python3 scripts/smart-cleanup-plan.py --json reports/smart-cleanup-plan.json --md reports/smart-cleanup-plan.md
          bash scripts/validate-all.sh
          python3 scripts/validate-topic-map.py
          python3 scripts/validate-site-shell.py
          node --check assets/site.js
          python3 scripts/test-logic.py

      - name: Commit changes
        if: ${{ github.event.inputs.apply == 'true' }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add metadata/index.json reports/*.json RULES.md
          if git diff --cached --quiet; then
            echo "No cleanup changes to commit"
          else
            git commit -m "chore(metadata): organize topics and clean display titles"
            git push
          fi

      - name: Upload audit reports
        uses: actions/upload-artifact@v4
        with:
          name: maagar-topic-audit
          path: reports/*
