name: Topic Organizer

on:
  workflow_dispatch:
    inputs:
      apply:
        description: "Apply changes to metadata/index.json"
        required: true
        default: "false"
        type: choice
        options:
          - "false"
          - "true"

permissions:
  contents: write

jobs:
  topic-organizer:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Run topic audit
        run: |
          mkdir -p reports
          python3 scripts/topic-organizer.py --report reports/topic-audit.json

      - name: Apply topic organization
        if: ${{ github.event.inputs.apply == 'true' }}
        run: |
          python3 scripts/topic-organizer.py --apply --report reports/topic-audit.json
          bash scripts/validate-all.sh
          python3 scripts/test-logic.py

      - name: Commit changes
        if: ${{ github.event.inputs.apply == 'true' }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add metadata/index.json reports/topic-audit.json
          if git diff --cached --quiet; then
            echo "No topic changes to commit"
          else
            git commit -m "chore(topics): organize metadata topics"
            git push
          fi

      - name: Upload topic audit
        uses: actions/upload-artifact@v4
        with:
          name: topic-audit
          path: reports/topic-audit.json
